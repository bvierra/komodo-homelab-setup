---
# Role-specific defaults for Komodo Core deployment
# Main configuration moved to inventory/all.yml
# Only role-internal behavior settings here

# Role behavior defaults
komodo_compose_service_name: "komodo-core"
komodo_systemd_service_enabled: true
komodo_health_check_retries: 5
komodo_health_check_delay: 10
