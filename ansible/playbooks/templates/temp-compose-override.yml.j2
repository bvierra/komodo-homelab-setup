# Temporary docker-compose override to enable user registration
# Used during initialization to create the first admin user
# This file is automatically removed after user creation

services:
  core:
    environment:
      KOMODO_DISABLE_USER_REGISTRATION: "false"
